#include <stdlib.h>
#include <commun.h>
#include <string.h>

void afficher_menu(menus_t * menu){
    switch (menu) {
        case 0: break;
        case 1: break;
        case 2: break;
        case 3: break;
    
        default: exit(EXIT_SUCCESS);
    }
}